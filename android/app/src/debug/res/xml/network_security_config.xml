<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config.
  Allows cleartext HTTP to the Android emulator host (10.0.2.2) and common
  dev hostnames so we can talk to a Django runserver during development.
  Production builds (release) keep Android's default cleartextTrafficPermitted=false.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
